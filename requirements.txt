# --- Core ---
streamlit==1.38.0          # stable UI build; supports st.rerun()
openai==1.42.0             # required to remove 'proxies' arg error
httpx==0.27.0              # compatible with OpenAI >=1.40

# --- File parsing ---
python-docx==1.1.2
pdfplumber==0.11.4
pdf2image==1.17.0          # for OCR fallback
pytesseract==0.3.10        # OCR engine wrapper
pillow==10.4.0             # image handling for OCR

# --- Environment ---
python-dotenv==1.0.1


